namespace backend.Entities;

public enum StatusEnum {
    Active = 1,
    Inactive = 2,
}

public enum SizeEnum {
    Kiosk = 1,
    StandardStore = 2,
    StreetStore = 3,
    Flagship = 4,
}

public enum TypeEnum {
    Own = 1,
    Franchise = 2
}